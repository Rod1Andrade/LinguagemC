#include <stdio.h>
#include <stdlib.h>

int main(void)
{
  
  /**
   * Podemos escrever funções que aceitam quantidade váriavel de parâmetros,
   * que podem ser de diversos tipos, como é o caso da famosa função printf("");
   * - Para declarar uma função com quantidade váriavel de parâmetros, basta colocar "..."
   *   como o último parâmetro na declaração da função
   *
   *   "Forma geral"
   *
   * <tipo> nome (lista parâmetros, ...)
   * {
   *    ... Corpo da função
   * }
   *
   * */
  return 0;
}
